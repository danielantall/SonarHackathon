fastapi==0.100.0          # FastAPI framework
uvicorn==0.22.0           # ASGI server for running FastAPI
sqlalchemy==2.0.20        # ORM for database interactions
python-dotenv==1.0.0      # For loading environment variables
pydantic==1.10.9          # Data validation and settings management
psycopg2-binary==2.9.8    # PostgreSQL database adapter (if using PostgreSQL)